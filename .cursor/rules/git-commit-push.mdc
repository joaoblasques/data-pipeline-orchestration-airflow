---
description:
globs:
alwaysApply: false
---
# Git Commit and Push Rule

After every 10 changes in the project, you must:

1. Add all files to staging using `git add .`
2. Create your own commit message (do not use a template or auto-generated message).
3. Commit the changes using `git commit`.
4. Push the commit to the GitHub remote using `git push`.

This rule applies to all files in the repository.
