---
description:
globs:
alwaysApply: false
---
# Branching Strategy

Always create a new branch for each feature or bugfix. Never commit directly to the `main` or `master` branch.

Branch names should be descriptive, e.g., `feature/data-ingestion`, `bugfix/typo-in-readme`.
